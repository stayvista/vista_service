server:
  port: 8084

spring:
  application:
    name: stayvista-search
  datasource:
    url: jdbc:mysql://127.0.0.1:13306/stayvista
    username: root
    password: root
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: filesystem:../db/migrations
