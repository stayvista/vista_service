server:
  port: 8085

spring:
  application:
    name: stayvista-ticket
  datasource:
    url: jdbc:mysql://127.0.0.1:23306/stayvista
    username: root
    password: root
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: filesystem:../db/migrations
