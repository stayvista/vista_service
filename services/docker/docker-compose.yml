name: stayvista-llm

services:
  llm:
    image: ollama/ollama:0.11.4
    container_name: stayvista-llm
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    environment:
      OLLAMA_KEEP_ALIVE: 30m
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:11434/api/tags >/dev/null"]
      interval: 10s
      timeout: 4s
      retries: 20

  llm-init:
    image: ollama/ollama:0.11.4
    container_name: stayvista-llm-init
    depends_on:
      llm:
        condition: service_healthy
    environment:
      OLLAMA_HOST: http://llm:11434
      OLLAMA_CHAT_MODEL: ${OLLAMA_CHAT_MODEL:-llama3.1:8b-instruct}
      OLLAMA_EMBED_MODEL: ${OLLAMA_EMBED_MODEL:-bge-m3}
    entrypoint:
      - /bin/sh
      - -lc
      - |
        set -e
        echo "[llm-init] pull chat model: ${OLLAMA_CHAT_MODEL}"
        ollama pull "${OLLAMA_CHAT_MODEL}"
        echo "[llm-init] pull embed model: ${OLLAMA_EMBED_MODEL}"
        ollama pull "${OLLAMA_EMBED_MODEL}"
        echo "[llm-init] complete"
    restart: "no"

volumes:
  ollama_data:
